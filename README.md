# kirmy.org

Personal project list. Live at **[kirmy.org](https://kirmy.org)**.

Plain HTML and CSS. No framework, no tracker.

```bash
python3 -m http.server 8080
```

Push `main` to publish via GitHub Pages. See [`AGENTS.md`](AGENTS.md).
